# Служебные токены: справка

> Материал для `rich_text`-слайда 4. Развёрнутая справка по служебным токенам ChatML-формата
> и ссылки, где их увидеть вживую.

## Зачем они нужны

Модель работает с одной плоской последовательностью токенов. Чтобы она понимала структуру разговора —
где начало сообщения, кто его автор и где конец — чат-приложение или агент вставляет **служебные токены**:
специальные символы из словаря модели, которых нет в обычном тексте.

Ты их не печатаешь. Но ты увидишь их в токенизаторах и в логах запросов.

## Основные служебные токены (семейство ChatML)

| Токен | Что делает |
|---|---|
| `<|im_start|>` | Начало сообщения. Дальше указывается роль: `system` / `user` / `assistant`. |
| `<|im_sep|>` | Разделитель — отделяет роль и/или части сообщения. |
| `<|im_end|>` | Конец сообщения. |
| `<|endoftext|>` | Конец всей последовательности текста (встречается и в других токенизаторах). |

> ⚠️ У каждого семейства моделей свой формат и свой набор служебных токенов. `<|im_start|>` и `<|im_end|>`
> характерны для ChatML (OpenAI и совместимых). Другие модели используют свои маркеры.

## Пример: разговор с «Василичем-5.7»

```
<|im_start|>system
Ты — «Василич-5.7», древняя языковая модель...
<|im_end|>
<|im_start|>user
Что умеет древний ИИ?
<|im_sep|>
<|im_start|>assistant
Отвечать, объяснять, помогать с кодом...
<|im_end|>
```

Читается так: началось системное сообщение → текст роли `system` → конец сообщения → началось сообщение
`user` → вопрос стажёра → разделитель → началось сообщение `assistant` → ответ модели → конец сообщения.

## Где увидеть вживую

- **tiktokenizer** (https://tiktokenizer.vercel.app): вставь текст, выбери шаблон `gpt-4o` — служебные
  токены появятся в разборе как отдельные токены (часто с пометкой «special»).
- **how-llms-work** (https://ynarwal.github.io/how-llms-work/): глава «Post-Training», блок
  «Conversation Token Format» — наглядно показано, как разговор собирается в плоскую
  последовательность токенов.

## Приватность (правило курса)

Весь этот текст — твой промпт вместе со служебными обёртками — уходит в облако, когда ты отправляешь
запрос. Поэтому в чат никогда не вставляем секреты и API-ключи: они хранятся только в `.env`
или в секретах, и не попадают ни в код, ни в сообщения.
