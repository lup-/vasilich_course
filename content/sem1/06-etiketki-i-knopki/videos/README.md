# Видео занятия 06 «Этикетки и кнопки»

## Список роликов

| Файл | Движок | Слайд | Что показывает |
|---|---|---|---|
| `manim/06-iz-teksta-kartinka.py` | Manim (ManimCE) | 3 | Из текста — картинка через диффузию (по описанию обучения): промпт над рядом этикеток → прямой процесс (5 степеней зашумления: шаг 0/250/500/750/1000, копия сдвигается вправо и пикселезуется, блочный шум = блочная пикселизация); обратный процесс/обучение (этап 2: модель обучается на 4 этикетках (порошок, глицерин, мыло, порошок-2); блок промпта (бежевый, чёрный текст, две строки, без слова «ПРОМПТ:») влетает в U-Net слева вместе с картинкой без стрелок (скорость влёта равна скорости вылета, выходные появляются без фейда); веса U-Net — круги (при нуле веса точки, с ростом веса радиус и цвет меняются), накапливаются между шагами и не сбрасываются; для 4 степеней зашумления 250/500/750/1000 две попытки разделения — неверная (предсказанный шум отличается от эталона, «чистая» сохраняет следы шума), затем после перестройки весов точная (шум совпадает, картинка чистая)); само начало сцены 2 появляется рамка «ЦЕЛЕВОЙ РЕЗУЛЬТАТ» вокруг обеих картинок шума (нижняя граница чуть ниже); после 4-х станций рамка переезжает к чистой картинке, заголовок меняется на «ЭТАП 3: МОДЕЛЬ ГОТОВА, ГЕНЕРАЦИЯ», на вход подаётся случайный шум + запрос (подпись «запрос»), модель генерирует хозяйственное мыло — на выходе изображение в рамке (рамка поднята и подпись снизу; пауза ~2 с перед переходом); генерация (шум → пошаговое вычитание шума → этикетка); два варианта из разного шума; вывод: шум + промпт → диффузия → картинка, мета-промпт задаёт границы |
| `manim/06-vyzov-instrumentov.py` | Manim (ManimCE) | 8 | Вызов инструментов: руки для модели — 4 этапа: (1) «RAG НЕ ВСЕГДА ПОДХОДИТ»: запрос уходит в модель («ДУМАЮ...» мигает), из векторной базы валятся ордера (тускнеют), подписи (+100 КГ СОДЫ — зелёные, −20 КГ ЦЕМЕНТА — красные) сваливаются кучей к Василичу (куча сверху вниз); на третьей накладной Василич злеет, индикатор «КОНТЕКСТНОЕ ОКНО» под моделью доходит до 90% (жёлтый), чипы «А ИНВЕНТАРИЗАЦИЯ?» и др. ровной стопкой поверх кучи переполняют окно (>100%, красный мигающий, заливка за правый край), Василич супер-злой, ошибки («превышено время ожидания», «недостаточно баланса») мигают ~3 с; (2) «ДАННЫЕ ЖИВЫЕ»: погода/новости/датчики/остатки на складе зачёркиваются по одному; (3) «ВЫЗОВ ИНСТРУМЕНТА ПОД КАПОТОМ»: колонки UI / ОБВЯЗКА (HARNESS) / МОДЕЛЬ (LLM) без фона, снаружи под обвязкой — блоки «СКРИПТ GET_SKLAD.PY» (pandas: read_csv + groupby с суммой) и «JSON ИНСТРУМЕНТА» с командой запуска `python get_sklad.py` — в ширину колонки; копия запроса летит в обвязку, к ней добавляются чип «СИСТЕМНЫЙ ПРОМПТ СКЛАДА» (прилетает снизу) и чип «get_sklad tool: остатки склада» (вылетает из json-блока) — оба без спец-токенов; тёмным чипам — светлый бордюр; пакет идёт в LLM; ответ со спец-токенами (`<|im_start|>`, `<|plugin_call|> get_sklad({})`, `<|im_end|>`) возвращается в обвязку, сегмент вызова подсвечивается, окно модели очищается; вертикальная стрелка от чипа ответа к заголовку скрипта; скрипт вспыхивает ярко-зелёным с чёрным текстом, в UI под запросом мигает «ЗАПУСК СКРИПТА...» (остаётся до конца сцены); скрипт тускнеет, в обвязку возвращается json-ответ, от него отделяется чип «РЕЗУЛЬТАТ РАБОТЫ СКРИПТА» и встаёт в UI под надписью запуска; все пять чипов обвязки выстраиваются стопкой и летят в модель копией; ответ «НА СКЛАДЕ ПОРОШОК И СОДА. ЦЕМЕНТА НЕТ» формируется под стопкой, окно модели очищается; ответ копией возвращается в обвязку под стопку, второй копией уходит в UI; (4) вывод «ИНСТРУМЕНТ — РУКИ МОДЕЛИ» |

## Запуск

### «Из текста — картинка» (ManimCE)

```bash
python -m manim render -ql -p --format mp4 --resolution 1920,1080 --fps 30 --seed 6 06-iz-teksta-kartinka.py IzTekstaKartinka
```

- **Что проверять:** поток читается: (1) прямой процесс — промпт над рядом этикеток; слева чистая
  (`шаг=0`), затем копия сдвигается вправо и пикселезуется, процедура повторяется — в финале **5 изображений**
  (`шаг=0, 250, 500, 750, 1000`), всё блочнее и шумнее; размер «пикселей» шума **равен** размеру пикселей
  пикселизации (общая сетка блоков); (2) обучение (этап 2) — само начало сцены 2: рамка «ЦЕЛЕВОЙ РЕЗУЛЬТАТ» вокруг обеих картинок шума (нижняя граница чуть ниже); затем модель учится на 4 разных этикетках (порошок, глицерин, мыло, порошок-2); блок промпта (бежевый, чёрный текст, две строки: название этикетки + серийный номер (напр. «БАННОЕ», «ПАР», «МАКС»), без слова «ПРОМПТ:») стоит над входной картинкой и вместе с ней долетает до центра U-Net, входная картинка мгновенно исчезает и на её месте разлетаются чистая и шум (промпт гаснет плавно), без стрелок (скорость влёта равна скорости вылета, выходные появляются без фейда); веса U-Net внутри — круги (сетка 11 рядов; изначально точки, при «ВЕСА ПЕРЕСТРАИВАЮТСЯ» растут и зеленеют, НАКАПЛИВАЮТСЯ между шагами и не сбрасываются); при НЕ СОВПАЛО рисуется изогнутая стрелка от центра верхней границы «НЕ СОВПАЛО» в центр U-Net, затем веса обновляются; подпись «ВЕСА ПЕРЕСТРАИВАЮТСЯ» появляется над картинками с шумом чуть правее стрелки; все картинки второй сцены одинакового размера; первая сцена быстрее, вторая — чуть медленнее; итог (СОВПАЛО / НЕ СОВПАЛО) показывается в мигающем прямоугольнике: красный фон при НЕ СОВПАЛО, зелёный при СОВПАЛИ, чёрный текст; для каждой степени зашумления 250/500/750/1000 сначала неверная попытка: предсказанный шум отличается от эталонного (другой цвет/прозрачность), а «чистая» сохраняет следы шума; затем «ВЕСА ПЕРЕСТРАИВАЮТСЯ» (круги растут и зеленеют) и вторая попытка точная: шум совпадает (СОВПАЛИ), картинка чистая; эталонный шум стоит справа, правее разделённого шума; после 4-х станций рамка «ЦЕЛЕВОЙ РЕЗУЛЬТАТ» переезжает к чистой картинке, заголовок меняется на «ЭТАП 3: МОДЕЛЬ ГОТОВА, ГЕНЕРАЦИЯ», на вход подаётся случайный шум + запрос (подпись «запрос»), модель генерирует хозяйственное мыло — на выходе изображение в рамке (рамка поднята и подпись снизу; пауза ~2 с перед переходом); (4) генерация —
  шум → пошаговое очищение → этикетка; (5) два варианта из разного шума. Пикселизация — **реальная**
  (квадратные блоки, не круглые точки).
  Надписи читаемы, этикетки не накладываются друг на друга в кадре вариантов.
- **Стиль:** палитра `comic-style.md` (болотный/ржавый/пыльный + кислотно-зелёный люминофор), чёрный
  контур, надписи заглавными рубленым шрифтом. Пикселизация и шум — наглядное упрощение диффузии.
- Отрендеренный файл — `videos/06-iz-teksta-kartinka.mp4` (~84 с), указывается в `video_url` слайда 3
  (`video_type: direct`).

### Внешние ассеты (допустимы по `video-guide.md`, сообщаем явно)

| Файл | Откуда | Для чего |
|---|---|---|
| `videos/этикетка порошка.png` | папка `videos/` занятия 06 | Целевая этикетка «ПОРОШОК-МАКС»: прямой/обратный процесс (seed 6) и вариант «шум A» (seed 11) |
| `videos/этикетка порошка 2.png` | папка `videos/` занятия 06 | Альтернативный вариант из другого стартового шума — «шум B» (seed 23) |
| `videos/этикетка глицерина.png` | папка `videos/` занятия 06 | Этикетка глицерина: тренировочная выборка (этап 2) |
| `videos/этикетка мыла.png` | папка `videos/` занятия 06 | Этикетка хозяйственного мыла: тренировка (этап 2) и результат генерации «Этап 3» |
| `videos/Vasilych_calm.png` | папка `videos/` занятия 06 (1024×1024) | Спокойный Василич: третий столбец сцены «RAG НЕ ВСЕГДА ПОДХОДИТ» ролика «Вызов инструментов» |
| `videos/Vasilych_angry.png` | папка `videos/` занятия 06 (1024×1024) | Злой Василич: смена после третьей накладной в той же сцене |
| `videos/Vasilych_super_angry.png` | папка `videos/` занятия 06 (1024×1024) | Супер-злой Василич: смена при переливе контекстного окна в той же сцене |

Оба PNG — портретные (848×1264), вписаны в кадр 1920×1080 (высота ~6 юнитов). Пикселизация считается
в коде (PIL: даунскейл + NEAREST апскейл), шум — детерминированный по seed. Пути задаются в скрипте
относительно `videos/`.

### «Вызов инструментов: руки для модели» (ManimCE)

```bash
python -m manim render -ql -p --format mp4 --resolution 1920,1080 --fps 30 --seed 6 06-vyzov-instrumentov.py VyzovInstrumentov
```

- **Что проверять:** 4 этапа (~98 с). Вступление RAG (ч. 1): три столбца, куча растёт сверху вниз,
  индикатор зелёный → жёлтый (90%) → красный (>100%, вылет за правый край кадра), чипы ошибок
  мигают ~3 с перед переходом. Ч. 2: четыре пункта зачёркиваются по одному, мост к вызову инструмента.
  «Под капотом»: читается поток UI → обвязка → LLM → обвязка → UI; спец-токены видны и подсвечены
  (<|plugin_call|> get_sklad({})), стрелки от сегмента — к скрипту и к UI, статусы в UI сменяются
  («ЗАПУСК СКРИПТА...» мигает → «РЕЗУЛЬТАТ РАБОТЫ СКРИПТА»), финальный ответ без спец-токенов.
  Добавлены три карточки-пояснения (полноэкранные вставки с текстом): (1) «Обвязка добавляет к запросу
  системный промпт и описания инструментов» — после появления чипов в обвязке; (2) «Модель вызывает
  инструмент спец-тегом <|plugin_call|> (у других — <tool_call> (Qwen), <|python_tag|> (Llama))» — после
  ответа модели со спец-токенами; (3) «Обвязка по тегу вызывает инструмент и передаёт результат модели»
  — после жёлтой рамки вокруг сегмента вызова. Финальная мысль «ИНСТРУМЕНТ — РУКИ МОДЕЛИ: ВЫЗВАЛ →
  РЕАЛЬНЫЙ РЕЗУЛЬТАТ → ОТВЕТ ПО ДЕЛУ».
- **Стиль:** как в первом ролике занятия. Сюжетная рамка только в завязке (дежурный на пульте).
- Отрендеренный файл — `videos/06-vyzov-instrumentov.mp4` (~98 с), указывается в `video_url` слайда 8
  (`video_type: direct`).

## Общее

- Оба ролика — технические (Manim), по `video-guide.md`: объясняют механизм, без лиц и сюжета.
- Детерминизм: в командах рендера фиксированный `--seed 6`.
