# Занятие 11. «Разбираем архив: форматы и ноутбуки»

> Семестр 1 · Блок 3 «Пыльные архивы» · Курс 04, лекции 01–03 + задание 01 · ~2 ч

## Миссия занятия

> «Вот и дошли до архивов, молодежь! На складе нашли диски: таблицы Excel/CSV, JSON-логи датчиков,
> PDF-накладные — вся история завода. Разбирай завалы: открой первую таблицу, посчитай, отсортируй,
> сохрани обратно — и доложи, что внутри. Только смотри: что машина посчитает — сам проверь,
> не доверяй черту на слово!» — Прораб Василич

**Задача квеста:** понять, что данные «вокруг нас» и в каких форматах они лежат (CSV, JSON, Excel, PDF);
познакомиться с ноутбуком как средой работы через агента; через агента открыть первую таблицу из архива,
посчитать и отсортировать её, сохранить обратно.

**Итог занятия:** студент объясняет, чем CSV/JSON/Excel отличаются друг от друга и как определить формат,
что такое ноутбук и зачем он нужен, и сдаёт Василичу разобранный архив: таблица прочитана, посчитана,
отсортирована и сохранена обратно (сверка по контрольной сумме).

---

## Слайды

| № | Тип | Название | Краткое содержание |
|---|-----|----------|--------------------|
| 1 | `image_group` | Комикс «Разбираем архив» | 5 панелей: склад, кладовщик-зумер с коробкой дисков → форматы (CSV/JSON/Excel/PDF) → приказ Василича → Алиса про данные → задача «открыть первую таблицу» |
| 2 | `rich_text` | «Миссия: разбери завалы» | Приказ Василича + задача квеста + итог занятия |
| 3 | `rich_text` | «Данные вокруг нас» | Лекция 01: типы данных, что умеет ИИ, формула задания агенту (что есть → что сделать → результат → как проверить); Mermaid |
| 4 | `video` | «Форматы данных: CSV, JSON, Excel» | Manim ~75 с: файлы архива → CSV → JSON → Excel → PDF (кратко) → вывод «формат подсказывает, как читать» |
| 5 | `rich_text` | «CSV — таблица в тексте» | Лекция 02: пример `sales.csv`, разделители (`;`/`,`), открывается блокнотом |
| 6 | `rich_text` | «JSON — структура для машин» | Лекция 02: пары «ключ: значение», вложенность, веб-API |
| 7 | `rich_text` | «Excel — таблица с форматированием» | Лекция 02: листы, формулы, стили; PDF — не таблица; таблица «другие форматы» |
| 8 | `quiz` | «Какой формат?» | Контрольная точка: распознать формат по фрагменту файла |
| 9 | `resources` | «Архив завода» | Скачивание: `arhiv-zavoda.zip` (CSV + JSON + Excel) + памятка |
| 10 | `rich_text` | «Jupyter: среда через агента» | Лекция 03: ноутбук, ячейки кода/Markdown, порядок запуска, «сначала спроси, потом запускай» |
| 11 | `jupyter` | «Попробуй ноутбук» | Hands-on из задания 01: создать таблицу, посчитать «итого», проверить сумму |
| 12 | `rich_text` | «Проверка, экономика, приватность» | Контрольная сумма; 2 часа вручную vs 5 минут агентом; облако vs `.env` |
| 13 | `open_question` | «Открой таблицу и проверь» | Главная практика через агента: открыть CSV из архива, посчитать и отсортировать, сохранить; авто-проверка по контрольному ответу |
| 14 | `image_group` | Комикс-развязка «Первая таблица» | 4 панели: таблица прочитана/посчитана/сохранена, Василич сверяет по накладной; хук на занятие 12 (глицерин, сводки и учёт) |
| 15 | `image` | Мем «разбираем архив» | Разрядка после практики (человек проверяет картинку) |

---

## Детали по слайдам

### 1. image_group — Комикс «Разбираем архив»

- **Назначение:** завязка занятия: продолжение хука финала занятия 10 («Теперь в архив!»). На складе
  кладовщик-зумер (типаж из `world-bible.md`) выкатывает коробку дисков с данными; Василич приказывает
  разбирать; Алиса объясняет, что данные вокруг нас и у каждого формата свой язык. См. `comic.md`, сцена 1.
- **Содержание:**
  - `title`: «Разбираем архив»
  - `images`: 5 панелей (кадры 1.1–1.5 из `comic.md`, файлы `slide-11/01.png–05.png`)
  - `show_frames`: true
  - `description`: «Склад-архив завода. Кладовщик-зумер выкатывает тележку с пыльной коробкой „АРХИВ. ДИСКИ“:
    внутри CSV-таблицы, JSON-логи датчиков и PDF-накладные. Василич: „Разбирай завалы, молодежь!“.
    Алиса объясняет: данные вокруг нас — таблицы, логи, накладные — и у каждого формата свой язык.
    Задача квеста: открыть первую таблицу, посчитать, отсортировать, сохранить обратно.»
- **Хук:** сцена 2 развязки (слайд 14) подводит к занятию 12 («Сводки и учёт: pandas» — глицерин).

### 2. rich_text — «Миссия: разбери завалы»

- **Назначение:** миссия занятия от имени Василича (п.2 шаблона): разобрать архив — понять форматы,
  попробовать ноутбук, открыть первую таблицу через агента. Одна мысль: разбираем завалы и проверяем
  каждую цифру.
- **Содержание** (`markdown`):

```markdown
**Миссия: разбери завалы.**

«Вот и дошли до архивов, молодежь! На складе нашли диски: таблицы Excel/CSV, JSON-логи датчиков,
PDF-накладные — вся история завода. Разбирай завалы: открой первую таблицу, посчитай, отсортируй,
сохрани обратно — и доложи, что внутри. Только смотри: что машина посчитает — сам проверь,
не доверяй черту на слово!»

**Задача квеста:** понять, в каких форматах лежат данные архива (CSV, JSON, Excel) и как определить
формат; познакомиться с ноутбуком — средой, в которой агент пишет и запускает код; через агента
открыть первую таблицу из архива, посчитать и отсортировать её и сохранить обратно.

**Итог занятия:** разобранный архив — таблица прочитана, посчитана, отсортирована и сохранена обратно,
сверена по контрольной сумме; плюс понимание, с какими форматами мы теперь работаем.
```

### 3. rich_text — «Данные вокруг нас»

- **Назначение:** первое понятие от имени Алисы (п.7 шаблона), по лекции 01 курса-04: данные вокруг нас,
  что умеет ИИ с данными, главное — модель не знает ваши файлы сама по себе; формула задания агенту.
  Mermaid — «наглядность» прямо в тексте (п.8).
- **Содержание** (`markdown`):

````markdown
**Данные вокруг нас.**

«Данные, деточка, — это информация в какой-то форме: таблица с продажами, накладная, логи датчиков.
Помню-помню… раньше Элечка эту рутину щёлкал на раз-два, имба был. А как сломался — остались бы
с архивами один на один, всё руками-руками. Ох, ёшкин кот. Но древние фиксики ещё живы — им и отдаём.

Фиксик с данными умеет: писать код, запускать его, объяснять, что получилось, и собирать отчёты.
Но главное — **он не знает твоих файлов сам по себе**: видит их только когда ты их даёшь. Без б, деточка.

Как сказать ему задачу? Формула из четырёх пунктов:

1. **Что есть** — файл и его формат;
2. **Что сделать** — посчитать, отсортировать, свести;
3. **Какой результат** — сохранить, показать, нарисовать;
4. **Как проверить** — контрольная сумма, сходимость цифр.

```mermaid
flowchart LR
  Z["ЗАДАЧА СЛОВАМИ:\nчто есть → что сделать → результат → проверка"] --> A["АГЕНТ ПИШЕТ КОД"]
  A --> K["КОМПЬЮТЕР ЗАПУСКАЕТ"]
  K --> R["РЕЗУЛЬТАТ: таблица, цифры, файл"]
  R --> P["ТЫ ПРОВЕРЯЕШЬ"]
```

Подробнее — [разбираем архив](@razbor-arhiva).»
````

### 4. video — «Форматы данных: CSV, JSON, Excel»

- **Назначение:** наглядно показать три формата архива после рассказа Алисы: CSV — «таблица в тексте»,
  JSON — «структура для машин», Excel — «таблица с форматированием»; кратко — PDF как особый случай.
  Технический ролик (Manim). По договорённости — сценарий + набросок кода (`videos/`), рендер позже.
- **Содержание:**
  - `title`: «Форматы данных: CSV, JSON, Excel»
  - `video_url`: `videos/11-formaty-dannyh.mp4` (отрендеренный файл — будет)
  - `video_type`: `direct`
  - `description`: «Технический ролик: на экране три файла архива — `tovary-sklada.csv`,
    `datchiki-ceh7.json`, `ostatki-sklada.xlsx`. Каждый «открывается» по-своему: CSV — строки через
    запятую (разделитель виден отдельно), JSON — фигурные скобки и пары „ключ: значение“ (вложенные
    списки, отвечают веб-API), Excel — сетка ячеек, вкладки листов и формула `=СУММ(A1:A10)`.
    PDF показан кратко: страницы „запечатаны“, текст как картинка. Вывод: „ФОРМАТ ПОДСКАЗЫВАЕТ,
    КАК ЧИТАТЬ ФАЙЛ“. Программа-сценарий: `videos/manim/11-formaty-dannyh.py` (см. `video-guide.md`).»
- **План ролика (примерный сценарий, ~75 с):** см. раздел «Видео: план ролика» ниже.

### 5. rich_text — «CSV — таблица в тексте»

- **Назначение:** формальное объяснение после видео (п.7 шаблона), по лекции 02 курса-04: CSV — самая
  простая таблица в тексте; разделитель — частая ловушка. Одна мысль: CSV = «голые» данные, без
  форматирования, легко открыть и передать.
- **Содержание** (`markdown`):

````markdown
**CSV — таблица в тексте.**

«Самый простой формат, деточка, прям чиль: таблица, записанная обычным текстом. Каждая строка —
одна запись, значения через запятую. Первая строка — часто названия столбцов.

```csv
date,region,amount
2026-01-05,Москва,1500
2026-01-05,СПб,900
```

Открывается блокнотом, Excel, любой таблицей. Компактный — легко передавать между системами.
Не хранит ни цветов, ни формул — только «голые» данные.

Ловушка — **разделитель**: в русской локали Excel часто пишет `;` вместо `,` (запятая занята
десятичной точкой). Прочитал криво — первым делом проверь разделитель.

Подробнее — [разбираем архив](@razbor-arhiva).»
````

### 6. rich_text — «JSON — структура для машин»

- **Назначение:** формальное объяснение по лекции 02: JSON — формат для вложенных данных; пары
  «ключ: значение», объекты и списки внутри; основной формат веб-API. Одна мысль: JSON неудобен
  человеку, зато идеален для машин — и агент легко превратит его в таблицу.
- **Содержание** (`markdown`):

````markdown
**JSON — структура для машин.**

«Когда данных больше и они вложенные — приходит JSON, деточка. Данные в фигурных скобках,
как пары „ключ: значение“. Помню, как первый раз увидела — ох, ёшкин кот, скобки в скобках!

```json
{
  "city": "Москва",
  "temperature": -3,
  "forecast": [
    {"day": "2026-01-01", "temp": -3},
    {"day": "2026-01-02", "temp": -5}
  ]
}
```

Внутри — объекты внутри объектов, списки внутри списков. Человеку читать неудобно, а машине —
идеально. Именно JSON отвечают сайты и веб-API: курсы валют, погода, логи датчиков.

Фиксик легко превратит JSON в таблицу: скажи ему „сделай из этого CSV“ — красава будет.

Подробнее — [разбираем архив](@razbor-arhiva).»
````

### 7. rich_text — «Excel — таблица с форматированием»

- **Назначение:** формальное объяснение по лекции 02: Excel — таблица с «обёрткой» (листы, формулы,
  стили); PDF — не таблица; как определять формат. Одна мысль: форматирование удобно человеку,
  мешает машине; PDF читать как данные нельзя.
- **Содержание** (`markdown`):

````markdown
**Excel — таблица с форматированием.**

«А это, деточка, — таблица с „обёрткой“: листы, формулы, цвета, объединённые ячейки. Человеку удобно,
а фиксику — кринж. Читая через код, указывай, **какой лист** нужен: их может быть несколько.

Excel — „точка входа“ заводских данных: бухгалтерия, склад, логистика — всё здесь.

А вот **PDF — не таблица**: страницы „запечатаны“, текст и таблицы лежат как картинка. Извлекать
цифры оттуда — отдельная история (разберём позже). Если расширение не совпадает с содержимым —
доверяй содержимому.

| Формат | Что это | Где встречается |
|---|---|---|
| TSV | таблица через табуляцию | выгрузки из баз |
| XML | старый структурированный | наследие |
| Parquet | бинарный, колоночный | большие данные |
| HTML | веб-страницы | скрейпинг |

Подробнее — [разбираем архив](@razbor-arhiva).»
````

### 8. quiz — «Какой формат?»

- **Назначение:** контрольная точка по теории форматов (лекция 02). Материал — `files/quiz-formaty.md`.
- **Содержание:**
  - `question`: «Файл из архива начинается вот так: `[{"sensor": "temp", ...}, {"sensor": "vlag", ...}]`.
    Какой это формат и почему?»
  - `markdown`: «Выбери один правильный вариант. Термины занятия — [в справке](@razbor-arhiva).»
  - `options`:
    - { text: «JSON: фигурные скобки и пары „ключ: значение“, внутри — вложенные объекты и списки», is_correct: true }
    - { text: «CSV: это таблица со строками и запятыми», is_correct: false }
    - { text: «Excel: файл таблиц Microsoft, его видно по фигурным скобкам», is_correct: false }
    - { text: «PDF: документ, у которого все страницы начинаются с фигурных скобок», is_correct: false }
  - `explanation`: «Это JSON: данные записаны в фигурных скобках как пары „ключ: значение“ (`"sensor":
    "temp"`), а сами записи — вложенный список. Так устроены логи датчиков и ответы веб-API. CSV — строки
    с запятыми и заголовками, Excel — таблица с листами и формулами, а PDF — „запечатанный“ документ,
    не данные.»

### 9. resources — «Архив завода»

- **Назначение:** раздать материалы практики: архив завода (три файла трёх форматов) и памятку
  «как разбирать архив через агента». Данные нужны для главной практики (слайд 13).
- **Содержание:**
  - `title`: «Архив завода»
  - `description` (Markdown): «Это архив завода — то, что нашли на складе. Скачай оба файла.
    `arhiv-zavoda.zip` распакуй в папку `arhiv-zavoda` — внутри три файла трёх форматов:
    `tovary-sklada.csv` (таблица товаров склада), `datchiki-ceh7.json` (логи датчиков цеха №7),
    `ostatki-sklada.xlsx` (остатки склада, лист „Остатки“). Памятка — подсказка, как объяснять агенту
    задачу и как проверять результат. Всё понадобится на слайде 13.»
  - `files`:
    - { name: `arhiv-zavoda.zip`, description: «Архив завода: CSV + JSON + Excel (3 файла)» }
    - { name: `pamyatka-razbor-arhiva.md`, description: «Памятка: как разбирать архив через агента и проверять результат» }

### 10. rich_text — «Jupyter: среда через агента»

- **Назначение:** формальное объяснение по лекции 03 курса-04: ноутбук — файл `.ipynb` с ячейками кода
  и текста; код и результат хранятся вместе; запуск сверху вниз; «сначала спроси, потом запускай».
  Одна мысль: ноутбук — твоё рабочее место, ты в нём всё понимаешь и проверяешь.
- **Содержание** (`markdown`):

```markdown
**Jupyter: среда через агента.**

«Куда складывать код и результат, деточка? Ох, помню-помню… Есть для этого ноутбук — файл `.ipynb`,
а в нём ячейки двух типов:

- **ячейка кода** — Python: запустил — результат печатается под ней;
- **ячейка текста** (Markdown) — пояснения, заголовки, формулы.

Код и результат хранятся **вместе**: можно посмотреть, что делал агент и что получилось, перезапустить
и убедиться, что ничего не сломается. Запуск — сверху вниз: не запустил ячейку с данными —
и `NameError` («df не найден»).

Главное правило: ноутбук — твоя территория. **Сначала спроси, потом запускай:** „что делает эта
ячейка?“, „покажи первые 5 строк“, „проверь, что итог сходится“. Хороший ноутбук отвечает на три
вопроса: что делаем, что получили, какие выводы.

Подробнее — [разбираем архив](@razbor-arhiva).»
```

### 11. jupyter — «Попробуй ноутбук»

- **Назначение:** hands-on по заданию 01 курса-04: студент сам (или через агента) пробует ноутбук —
  создаёт таблицу, добавляет столбец «итого», считает сумму и проверяет её на калькуляторе.
  Интерактивная разминка перед главной практикой.
- **Содержание:**
  - `title`: «Попробуй ноутбук»
  - `kernel`: `pyodide`
  - `task` (Markdown): «Это ноутбук — твоё рабочее место на курс. Выполни по порядку:
    1) запусти ячейку (Shift+Enter) — появится таблица;
    2) добавь ячейку и попробуй посчитать: `df["итого"].sum()`;
    3) проверь сумму на калькуляторе: сходится ли с таблицей?
    4) ответь себе: что делает каждая строчка? Не знаешь — спроси у агента словами,
    и он объяснит.»
  - `code`: начальный код (создание таблицы и столбца «итого», по заданию 01):

```python
import pandas as pd

df = pd.DataFrame({
    "название": ["Книга", "Ручка", "Тетрадь", "Карандаш"],
    "цена": [500, 40, 90, 30],
    "количество": [2, 5, 3, 1],
})
df["итого"] = df["цена"] * df["количество"]
df
```

  - `description`: «Ядро — `pyodide` (Python в браузере). Клетки автосохраняются в прогресс.»
- **Примечание:** если pandas недоступен в `pyodide`-ядре, начальный код заменяем на простые списки
  без библиотек (например, словарь и цикл для подсчёта «итого») — учебная суть не меняется.

### 12. rich_text — «Проверка, экономика, приватность»

- **Назначение:** сквозные темы курса применительно к разбору архивов: проверка результата
  (контрольная сумма), экономика автоматизации (ручная работа vs агент), приватность данных
  (облако vs `.env`). Одна мысль: разобранный архив — фундамент отчёта, но каждая цифра проверяется.
- **Содержание** (`markdown`):

```markdown
**Проверка, экономика, приватность.**

«Разбор архивов — работа, которую раньше делали руками часами. Вот что помнить:

1. **Проверяем.** Машина посчитала — ты пересчитай: общая стоимость должна сойтись с суммой строк.
   Не сошлось — читала не тот файл или не тот столбец. Не доверяй машине на слово.
2. **Считаем.** Таблица на 5 минут агенту против 2 часов руками. А контрольная сумма — ещё минута,
   и она бережёт от „вранья“ на миллионы.
3. **Секреты — в `.env`.** Свои файлы и ключи не отправляем в чужие чаты: где лежат данные, туда
   и смотрим. Чужие облачные сервисы хороши для учебных файлов, секреты — не туда.

Польза: разобранный архив — это фундамент для отчёта дирекции в конце семестра.

Подробнее — [разбираем архив](@razbor-arhiva).»
```

### 13. open_question — «Открой таблицу и проверь»

- **Назначение:** главная практика-контрольная точка занятия: студент словами описывает агенту задачу —
  открыть CSV из архива, посчитать и отсортировать, сохранить обратно — запускает её на своём
  компьютере и отвечает на контрольный вопрос; ответ проверяется **автоматически** по эталону
  (`correct_answers`). Данные архива генерируем мы, поэтому ответ известен заранее (сумма 47 800).
  Материал — `files/zadanie-pervaya-tablica.md` + `files/pamyatka-razbor-arhiva.md`.
- **Содержание:**
  - `title`: «Открой таблицу и проверь»
  - `markdown` (задание): «Разбери архив через агента и проверь результат.
    1. Скачай архив со слайда 9 (`arhiv-zavoda.zip`) и распакуй в папку `arhiv-zavoda`.
    2. Опиши агенту словами (формула — в памятке): открой `tovary-sklada.csv`, покажи первые 5 строк
       и столбцы; добавь столбец „итого“ = цена × количество; посчитай общую сумму; отсортируй по цене
       по убыванию; сохрани результат обратно в CSV. Потом открой `datchiki-ceh7.json`
       и `ostatki-sklada.xlsx` (лист „Остатки“) и покажи, что внутри.
    3. Проверь своими глазами: сумма „итого“ сходится с общим итогом? Файл сохранён? Сверь цифры
       на калькуляторе.
    4. Впиши, чему равна общая стоимость всех товаров (пошаговая проверка — [как проверить задание](@kak-proverit-pervuyu-tablicu),
       агент для запуска — [opencode](@opencode-ustanovka)).»
  - `question`: «Чему равна общая стоимость всех товаров в архиве (сумма столбца „итого“
    по `tovary-sklada.csv`)?»
  - `correct_answers`:
    - «47800»
    - «47 800»
    - «47800 руб»
    - «47800 руб.»
    - «47 800 руб.»
    - «сорок семь тысяч восемьсот»
    - «сорок семь тысяч восемьсот рублей»
  - `congratulations`: «Верно! 1500×10 + 800×15 + 600×12 + 1200×5 + 950×8 = 47 800. Ты сверил сумму
    по контрольной цифре — как заведено на заводе. Архив разобран!»
  - `error_message`: «Не сошлось. Открой `tovary-sklada.csv`, посмотри столбцы цена и количество
    и проверь, что агент сложил именно произведение цена × количество по всем строкам.»
  - `hint`: «Посчитай вручную: 1500×10 + 800×15 + 600×12 + 1200×5 + 950×8 = ?»
  - `fuzzy_match`: true
  - `similarity_threshold`: 0.8
  - `show_correct_answer`: true

### 14. image_group — Комикс-развязка «Первая таблица»

- **Назначение:** развязка сюжета: таблица прочитана, посчитана и сохранена, Василич сверяет сумму
  по накладной и принимает работу; сквозная тема «не доверяй машине без проверки»; хук на занятие 12
  (сводки и учёт — «глицерин тает»). См. `comic.md`, сцена 2.
- **Содержание:**
  - `title`: «Первая таблица»
  - `images`: 4 панели (кадры 2.1–2.4 из `comic.md`, файлы `slide-11/06.png–09.png`)
  - `show_frames`: true
  - `description`: «Аппаратная. Стажёр открыл CSV: столбцы название/цена/количество, добавлен столбец
    „итого“, сумма 47 800. Василич сверяет с бумажной накладной: „Сходится — ни копейки не потеряли!“.
    Алиса напоминает: посчитала машина — проверь на калькуляторе. В финале заглядывает кладовщик-зумер:
    „В таблице склада странное: глицерин за год растаял, вагон!“ — хук на занятие 12 (сводки и учёт).»

### 15. image — Мем «разбираем архив»

- **Назначение:** разрядка после практики. Сквозная тема — данные и документы вокруг нас, но проверяем
  сами.
- **Содержание:**
  - `title`: «Мем: разбираем архив»
  - `image_url`: `memes/images/m00359.jpg` (кандидат; картинку проверяет человек глазами — по правилам
    курса)
  - `description`: «„Она: наверное, опять о документах своих думает…“ — а это мы после практики:
    разбираем архив и думаем о данных. Форматы поняли, ноутбук попробовали, первую таблицу разобрали —
    и всё проверили сами.»
- **Подбор:** `memes/pick_meme "гора бумаг, документы, архив, разбираться"` → кандидаты `m00359` (0.614),
  `m01912` (0.504), все `is_clean=true`. Картинку проверяет человек; при замене —
  `memes/images/{id}.jpg`.

---

## Видео: план ролика «Форматы данных: CSV, JSON, Excel»

> Технический ролик на Manim (ManimCE), `videos/manim/11-formaty-dannyh.py`. Движок — Manim, потому что
> ролик объясняет механизм (как устроены форматы и чем они отличаются). Стиль — из `comic-style.md`:
> палитра (болотный/ржавый/пыльный/бежевый + кислотно-зелёный люминофор), чёрный контур, надписи
> заглавными рубленым шрифтом. 1920×1080, 30 fps, длительность ~75 с, фиксированный `seed`. На момент
> сборки занятия ролик **не отрендерен** — есть план-сценарий и набросок кода (см. `videos/README.md`).

**Мысль ролика (одна):** у каждого формата свой язык — CSV «таблица в тексте», JSON «структура для
машин», Excel «таблица с форматированием», а PDF — документ, а не таблица; формат подсказывает,
как читать файл.

| № | Время | Кадр | Что происходит | Надписи в кадре |
|---|-------|------|----------------|-----------------|
| 1 | 0–9 с | Завязка | На экране три файла архива: `tovary-sklada.csv`, `datchiki-ceh7.json`, `ostatki-sklada.xlsx` | «АРХИВ ЗАВОДА: ФАЙЛЫ РАЗНЫХ ФОРМАТОВ» |
| 2 | 9–26 с | CSV | Файл «открывается» блокнотом: строки через запятую, первая — заголовок; разделитель подсвечен ржавым | «CSV — ТАБЛИЦА В ТЕКСТЕ», «СТРОКА = ЗАПИСЬ, ЗНАЧЕНИЯ ЧЕРЕЗ ЗАПЯТУЮ», «ОТКРЫВАЕТСЯ ЛЮБЫМ ТЕКСТОВЫМ РЕДАКТОРОМ», «ВНИМАНИЕ: РАЗДЕЛИТЕЛЬ — `;` ИЛИ `,`» |
| 3 | 26–40 с | JSON | Фигурные скобки, пары «ключ: значение», вложенный список | «JSON — СТРУКТУРА ДЛЯ МАШИН», «ПАРЫ „КЛЮЧ: ЗНАЧЕНИЕ“», «ВЛОЖЕННЫЕ ОБЪЕКТЫ И СПИСКИ», «ОТВЕЧАЮТ ВЕБ-API» |
| 4 | 40–55 с | Excel | Сетка ячеек, вкладки листов «Остатки»/«Поставки», формула `=СУММ(A1:A10)` | «EXCEL — ТАБЛИЦА С ФОРМАТИРОВАНИЕМ», «ЛИСТЫ · ФОРМУЛЫ · ЦВЕТА · СТИЛИ» |
| 5 | 55–65 с | PDF (кратко) | Страница «запечатана»: текст и таблицы как картинка | «PDF — ДОКУМЕНТ, НЕ ТАБЛИЦА», «СТРАНИЦЫ „ЗАПЕЧАТАНЫ“», «ИЗВЛЕКАТЬ ЦИФРЫ — ОТДЕЛЬНАЯ ИСТОРИЯ (ЗАНЯТИЕ 14)» |
| 6 | 65–75 с | Вывод | Крупная финальная надпись | «ФОРМАТ ПОДСКАЗЫВАЕТ, КАК ЧИТАТЬ ФАЙЛ», «CSV — ТЕКСТ · JSON — СТРУКТУРА · EXCEL — ТАБЛИЦА С ФОРМАТОМ» |

**Что НЕ показываем:** лиц и сюжета — ролик чисто технический, про устройство форматов. PDF показан
кратко, без углубления (полноценно — занятие 14). После ролика — слайды 5–7 (формальное объяснение
каждого формата), затем квиз (слайд 8) и раздача архива (слайд 9).

---

## Мемы

- **Слайд 15 (резюме, кандидат):** `m00359` — «она: наверное, опять о документах своих думает».
  Подбор: `memes/pick_meme "гора бумаг, документы, архив, разбираться"`.
  Кандидаты (все `is_clean=true`):
  - `m00359` (0.614) — «думает о документах» (фит к теме «разбираем архив / думаем о данных»);
  - `m01912` (0.504) — «не начинай войну, которую не сможешь закончить» + библиотечный кот
    (фит к теме «архив/библиотека» и «проверяем результат до конца»).
- Подбор субъективен: **картинку для слайда 15 проверяет человек глазами** (правило курса: не доверяй
  ИИ без проверки); после подтверждения оставляем `image_url: memes/images/m00359.jpg` или меняем на
  подтверждённый `memes/images/{id}.jpg`.

---

## Доп. информация

Блоки дополнительной информации (вкладка «Доп. информация» на платформе; слаги — общие на курс sem1).
Наполнение блоков — в `content/sem1/extra/<слаг>.md`.

| Слаг | Слайд | Ссылка на слайде |
|---|---|---|
| `razbor-arhiva` | 3, 5, 6, 7, 8, 10, 12 | «разбираем архив» |
| `opencode-ustanovka` | 12, 13 | «opencode» |
| `kak-proverit-pervuyu-tablicu` | 13 | «как проверить задание» |
